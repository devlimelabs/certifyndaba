import { CertificationNamePipe } from './certification-name.pipe';

describe('CertificationNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CertificationNamePipe();
    expect(pipe).toBeTruthy();
  });
});
